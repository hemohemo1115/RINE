<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
    <%= javascript_include_tag "window.js" %>
      <%#<h1>
        <%= link_to @user.name, "/rooms/show" %>
      <%#</h1>%>
        <% @rooms.each do |room| %>
            <li><%= link_to "ROOM#{room.id}", room_path(room) %></li>
        <% end %>
    </section>
    <h1>
        <%= current_user.name %>
    </h1>
  </aside>
</div>