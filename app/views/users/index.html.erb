<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
        <% if user != current_user %>
            <%= link_to user.name, user %>
        <% end %>
    </li>
  <% end %>
</ul>